describe('pixi-wrapper', function () {
	it('should be initialized', function () {
		expect(a.pixi).not.toBeFalsy();
	});
});

describe('topology', function () {
	it('should be initialized', function () {
		expect(a.tpl).not.toBeFalsy();
	});
});